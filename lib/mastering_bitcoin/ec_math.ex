defmodule MasteringBitcoin.ECMath do
  @moduledoc """
  Example 4-7. A script demonstrating elliptic curve math used for bitcoin keys

  NOTE: It seems that there isn't an Elixir wrapper or equivalent for
  Elliptic Curve Digital Signature Algorithm (ECDSA), so leave this exercise
  for now.
  """

  def run do
  end
end
